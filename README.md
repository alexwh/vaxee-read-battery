# vaxee-read-battery
Simply returns the percentage of a VAXEE/ZYGEN mouse. Tested on the NP-01 Ergo but should work with models from the same vendor ID (0x3057).

## Install
```sh
go install github.com/alexwh/vaxee-read-battery@latest
```
